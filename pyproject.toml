[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "voice-lab-local"
version = "0.1.0"
description = "本機音訊整理與 WAV 研究工具（第一版，無大型模型）"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
